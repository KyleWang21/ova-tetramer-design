# Superseded input

This input set was never submitted. It used an AF3-predicted 19K monomer to
recompute surface exposure, which is inconsistent with the frozen isolated
experimental 1OVA rSASA definition used by `surface_positions.txt`.

Use `experiments/e158_c4_iptm90_joint_surrogate_af3_seed1/` instead.
