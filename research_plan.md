# OVA P3-13R 四聚体研究计划

目标：得到在 AlphaFold 3 中稳定、可重复形成四链四聚体的单链序列，并先验证原始 P3-13R 的二聚体基线。

1. **二聚体基线（已完成）**：深 MSA 后 OVA 折叠可信，但 P3-13R×2 的 ipTM/PAE 未通过 AF3 二聚体门槛；不再假设 dimer-of-dimers。
2. **快速四聚体对照筛选（首个命中）**：由实验定义明确的平行四聚化 coiled-coil（GCN4-pLI、CC-Tet）直接驱动四聚体；`P3-GGGGS-pLI` 已通过首轮门槛，其余分片继续完成。
3. **AF3 判据**：四条 OVA 链必须构成一个连通复合物而非两个二聚体；关注整体 ipTM、最弱链的 pair ipTM、跨链接触、PAE、clash、OVA 单体折叠保持和多 seed 构型一致性。
4. **AI 热点设计（主线）**：把可靠 AF3 单链表面暴露度、MSA 保守性和 25 个低可信二聚模型的接触复现率合并定位三维热点；对独立热点实际运行 BindCraft（AF2 反向传播）与 ProteinMPNN，生成新的 OVA 结合界面序列。把 binder 与 OVA 组成同源融合链，再用 AF3 的 1/2/4 链竞争判断是单体自封闭、二聚体、四聚体还是非限定多聚体。
5. **确认**：首轮命中先用 pLI 域 MSA、更多 seed 和 1/2/4 链化学计量竞争复核；AI 候选执行同样盲测。最终输出序列、AF3 结构、分数表和关键图。

主要文献：AlphaFold 3（Nature 2024）；GCN4-pLI（Science 1993，PDB 1GCL）；CC-Tet（ACS Synthetic Biology 2012 / Nature Chemical Biology 2011）。

## 2026-08-31：低突变纯 OVA 四聚体阶段

目标：独立复核当前 42 突变候选，并获得相对 P3-13R 总突变数不超过 25、优先不超过 20 的纯 OVA 非共价同源四聚体。

1. 本机 A100 用 Protenix-v2、同源四链、无模板和单体 MSA 独立预测当前候选，统计四链连通、链对 ipTM/PAE、clash、界面接触及与 AF3 构象差异。
2. 以当前 AF3 四聚体为一个正骨架，同时保留从 P3-13R 重新起步的分支；显式拆分单体折叠、第一界面和第二界面损失，并加入相对 P3-13R 的可微分突变预算。
3. 火山 8×A100 与中卫 SLURM 8×A800 并行跑 tied 四链 AF2：四条链严格同序列、天然 Cys 固定、禁止新增 Cys、SIINFEKL 固定；比较固定四链骨架与仅保留单体模板的自由装配设置。
4. 仅保留离散序列突变数 ≤25 的候选，优先 ≤20；AF2 内部分数只用于筛选，成功标准仍是无模板 AF3 多 seed 的四链连通、最弱链接口、PAE、无 clash 和零链间二硫键。

## 2026-08-31：AF3 平均 ipTM >0.80 扩展阶段

目标：得到多个相对 P3-13R 严格少于 25 个突变、完整四链无模板 AF3 的 25 模型算术平均 ipTM >0.80 的纯 OVA 非共价候选。

1. 以 `17-B` 最佳 AF3 构象重新提取 AB/AC 两个强界面；另把较弱 AD 链对作为提高全局 ipTM 的补充状态，不沿用已偏离最终 AF3 构象的旧 D2 设计骨架。
2. 火山 8×A100 和中卫 8×A800 各运行八条 tied 同序列、两状态全长 AF2 可微分轨迹；在 `17-B`/`17-A` 基础上，以旧 42 突变界面作为 warm-start 氨基酸先验，预算为 20–24。
3. 保留轨迹中的多个 hard/soft 离散状态，而不只保留每张卡的最终序列；严格过滤新 Cys、SIINFEKL 改变和突变数 ≥25。
4. AF3 先以一个 seed×5 samples 快筛，再对领先候选扩展到五个独立 seed。最终门槛按 25 个模型的算术平均 ipTM 计算，同时要求四链连通、无 clash、零链间 S–S；单个最佳模型或中位数不算达标。

## 2026-08-31：OpenDDE后第二轮跨模型设计与AF3≈0.90目标（历史计划）

扩展目标：在现有 AF3/ProLIF/Protenix-v2/化学计量/Rosetta 核心证据链上，使用火山8×A100和中卫8×A800继续生成相对P3-13R不超过24个突变的纯OVA非共价同源四聚体；争取五个无模板AF3 seeds、共25个模型的算术平均ipTM接近或达到0.90，并把新增候选送入筛选规范 v1.1。OpenDDE/DockQ 只作可选诊断，不作为阻断阶段。

1. 从AF3、Protenix、OpenDDE和ProLIF中提取链置换归一化的共同界面作用；反复短于2.4 Å的具体残基/原子对及Rosetta埋藏未满足极性位点作为负设计信号。
2. 两路AI生成并行：一条把完整AF3四聚体作为坐标起点，在同一次四链tied-AF2/BindCraft式前向传播中同时约束单体折叠及两个观测界面；另一条使用AF3标签回灌的Hamming-kernel主动学习和跨界面块重组。ProteinMPNN只用于多骨架序列兼容性与重排，不作为成功证据。
3. 固定天然Cys和SIINFEKL，禁止新增Cys与亚基间二硫键；候选必须≤24突变、至少80%突变位于单体暴露表面，并在生成阶段剔除重复序列和已知高频原子冲突组合。
4. AF3先做1 seed×5 samples快筛；只有五模型均为完整C4、零链间S–S且分数有裕量者扩展到5 seeds×5 samples。`接近0.90`与`达到0.90`分开报告，绝不以最佳单模型或中位数替代25模型算术平均。
5. 新候选继续运行Protenix五seed、C1–C6负筛选、ProLIF、Rosetta和fail-closed总表；OpenDDE仅在已有结果可复用时作为可选诊断，不再作为required；最终同时交付排名CIF、FASTA、单一总表和研究日志。

硬件纠偏（2026-08-31）：完整4×386 AF2反传实测请求299.3 GB，单张80-GB A800无法运行。生成阶段因此改用同一连续tied序列参数上的双AF3界面多状态梯度求和（每步两次反传、一次共享更新），不采用先优化第一界面再优化第二界面的顺序法；完整四链装配仍只由无模板AF3多seed判定。

当前执行（2026-09-01）：正式OpenDDE 22体系×10 seeds已经完成；20条旧候选没有`ipTM>0.80`且几何合格的OpenDDE代表。中卫持续保持8张卡运行第五/第六波双界面共享参数设计，任务使用`ky-20260901-446`–`478`并由排队任务自动补卡。已同步35条轨迹并得到363条未被E158评估的合格AF2序列，当前第二池为64条AF2+64条代理、14–20突变。E158本机shard-0已完成15/16个完整五样本体系；17突变的`ova_body_c4_73`与`ova_body_c4_105`为两个严格seed1命中，其中73平均ipTM 0.850、平均设计突变接口覆盖0.765，105平均ipTM 0.810。73的seeds2–5已冻结为E165并优先排在105的E159之前；之后顺序运行E162回退库和E164交叉库。首个四链tied ProteinMPNN冒烟已证明API/四链输入可运行但2/2超出突变预算；提高P3偏置并扩为64样本的`ky-20260901-479`已排队。火山只有在c20250508队列空闲GPU严格大于10时才提交8卡任务，且单站并发不超过8卡；当前free=10仍不提交。未提交的E157因误用AF3预测构象重算表面而作废。

## 当前最终结果路径总览（2026-09-03）

现阶段应明确区分为 **2条主生成路径 + 1条备用生成路径 + 1条共用终审/交付路径**。集中说明、当前任务状态和权威文件见 [`docs/CURRENT_RESULT_ROUTES.md`](docs/CURRENT_RESULT_ROUTES.md)。主路径分别是中卫 E311→E312 的多骨架 tied-AF2 路线，以及火山 E314→E317 的 AF3 标签回灌主动学习路线；E283→E288 的 chainmapped bottom-k 路线只作为等待主线完成后的备用来源。所有路径最终都必须进入无模板 AF3 25模型、ProLIF、Protenix-v2、化学计量、Rosetta和统一 v1.1 表格；OpenDDE仅保留为可选诊断，不能把生成器分数当作最终成功证据。
